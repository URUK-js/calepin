{
  "name": "root",
  "private": true,
  "scripts": {
    "dev:solid": "pnpm recursive run dev --filter edytor-solid-demo",
    "build:solid": "pnpm recursive run build --filter edytor-solid-demo",
    "build:edytor": "pnpm recursive run build --filter edytor",
    "publish:edytor": "pnpm recursive run publish --filter edytor"
  }
}
